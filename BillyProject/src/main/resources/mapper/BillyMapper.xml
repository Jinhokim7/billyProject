<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.billy.DAO.IF_billyDAO">

	<insert id="insertOne" parameterType="com.billy.VO.BillyGoodsVO">
	<!-- 완성된 쿼리문
		insert into billyGoods
		values(#{id},#{gNum},#{cateNum},#{gName},#{gLoca},#{gStrDate},#{gEndDate},#{gPrice},#{gText})
	 -->
	 	insert into billyGoods
		values(a,2,#{cateNum},#{gName},#{gLoca},#{gStrDate},#{gEndDate},#{gPrice},#{gText})	
	</insert>

</mapper>